[project]
name = "backend"
version = "0.1.0"
description = "Hidden Catch backend (FastAPI + Celery puzzle generation pipeline)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.121.2",
    "pydantic-settings>=2.12.0",
    "sqlalchemy>=2.0.44",
    "psycopg[binary]>=3.2.1",
    "psycopg2-binary>=2.9.10",
    "uvicorn>=0.38.0",
    "alembic>=1.17.2",
    "boto3>=1.35.0",
    "celery>=5.5.3",
    "google-genai>=1.52.0",
    "google-cloud-vision>=3.11.0",
    "pillow>=12.0.0",
    "redis>=7.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.14.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py313"
exclude = [
    ".venv",
    "__pycache__",
    "migrations"
]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle (PEP 8)
    "F",  # pyflakes
    "W",  # warnings
    "I",  # isort (import 정렬)
    "C90",  # mccabe (복잡도)
]

ignore = [
    "F401",  # 사용하지 않는 import 제거하지 않음
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]

[tool.ruff.format]
# Black 스타일 포매팅
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint.isort]
known-first-party = ["app"]
force-sort-within-sections = true
split-on-trailing-comma = true
